import React from 'react';
import PropTypes from 'prop-types';

import Tile from '../Tile';

import './Ticket.scss';

function Ticket({}) {
  return (
    <Tile mixClass="ticket">
      Ticket
    </Tile>
  );
}

Tile.propTypes = {
};

export default Ticket;
